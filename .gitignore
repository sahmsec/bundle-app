node_modules

# macOS
.DS_Store

/.cache
/build
/app/build
/public/build/
/public/_dev
/app/public/build
/prisma/dev.sqlite
/prisma/dev.sqlite-journal
database.sqlite

.env
.env.*

# package-lock.json is committed on purpose — the Docker build uses `npm ci`,
# which requires it, and it makes builds reproducible.
yarn.lock
pnpm-lock.yaml

/extensions/*/dist

# Ignore shopify files created during app dev
.shopify/*
.shopify.lock

# Hide files auto-generated by react router
.react-router/


# Workflow files: pushing them needs a token with `workflow` scope.
# Add cron.yml via the GitHub web UI when wiring the daily rollup.
.github/workflows/
